================================================================================
АВТОМАТИЗИРОВАННЫЕ ТЕСТЫ - ДОКУМЕНТАЦИЯ
AI Translator & Critic - QA Engineering Test Suite
================================================================================

ОБЗОР
-----

В этом проекте реализована комплексная система автоматизированного тестирования,
включающая 27 тестов трёх уровней:

• 6 Unit Tests (юнит-тесты) - тестирование функции call_llm() в изоляции
• 9 Integration Tests (интеграционные тесты) - тестирование Flask маршрутов
• 9 Cypress e2e Tests (end-to-end тесты) - тестирование пользовательского интерфейса

Все тесты используют MOCKS (имитацию) вместо реальных API вызовов, поэтому они:
✓ Работают без доступа в интернет
✓ Не требуют API ключей
✓ Выполняются очень быстро
✓ Дают гарантированные результаты


================================================================================
НОВЫЕ ТЕСТЫ
================================================================================

Добавлено 16 новых тестов. Подробное описание каждого:


1. UNIT TESTS (Юнит-тесты)
==========================

Файл: tests/unit/test_app.py

1.1) test_call_llm_unicode_and_special_characters()
---------------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Обработка текста на разных языках: русский (Привет мир), китайский (你好世界),
    арабский (مرحبا العالم), эмодзи (👋 Hello 😀)
  • Корректность работы с Unicode символами

КАК РАБОТАЕТ:
  • Вызывает функцию call_llm() с различными Unicode текстами в режиме 'mock'
  • Проверяет, что функция возвращает корректный ответ для каждого текста
  • Затем тестирует режим 'auth' (с мокированным API) для одного Unicode текста

ПОЧЕМУ ВАЖЕН:
  Приложение должно корректно обрабатывать текст на любых языках, включая
  специальные символы и эмодзи. Этот тест гарантирует поддержку многоязычности.


1.2) test_call_llm_http_error_statuses()
----------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Корректная обработка HTTP ошибок: 400, 401, 403, 404, 500, 502, 503
  • Функция не должна падать, а должна возвращать понятное сообщение об ошибке

КАК РАБОТАЕТ:
  • Для каждого кода ошибки мокирует ответ сервера API
  • Вызывает call_llm() в режиме 'auth'
  • Проверяет, что функция возвращает строку с ошибкой (не выбрасывает исключение)

ПОЧЕМУ ВАЖЕН:
  Приложение должно корректно обрабатывать сбои API, показывая пользователю
  понятные сообщения об ошибках вместо краша.


1.3) test_call_llm_validates_request_payload()
----------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Корректность формирования HTTP запроса к API
  • Наличие и правильность заголовка Authorization (Bearer token)
  • Правильная структура JSON payload

КАК РАБОТАЕТ:
  • Устанавливает API ключ в окружение
  • Мокирует requests.post для перехвата вызова
  • Вызывает call_llm() в режиме 'auth'
  • Проверяет, что перехваченный вызов содержит:
    - Заголовок "Authorization: Bearer <key>"
    - Поле "model_name" в JSON
    - Поле "prompt" в JSON

ПОЧЕМУ ВАЖЕН:
  API сервер требует определённого формата запроса. Этот тест гарантирует,
  что приложение отправляет корректно сформированные запросы.


2. INTEGRATION TESTS (Интеграционные тесты)
==============================================

Файл: tests/integration/test_routes.py

2.1) test_all_languages_in_dropdown[lang]
-----------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Все 4 языка в выпадающем списке работают: Английский, Французский,
    Немецкий, Португальский
  • POST запрос с каждым языком возвращает 200 OK

КАК РАБОТАЕТ:
  • Использует @pytest.mark.parametrize для запуска теста 4 раза с разными языками
  • Отправляет POST запрос к приложению с текстом и языком
  • Проверяет статус код 200 и наличие перевода в ответе
  • Работает в режиме 'mock', поэтому не требует реального API

ПОЧЕМУ ВАЖЕН:
  Гарантирует, что все языки в интерфейсе обрабатываются корректно.


2.2) test_boundary_case_very_long_text()
----------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Приложение может обработать очень длинный текст (5000+ символов)
  • Не падает и не зависает при обработке большого объёма данных

КАК РАБОТАЕТ:
  • Создаёт строку из 5000 символов 'A'
  • Отправляет POST запрос с этим длинным текстом
  • Проверяет, что получен ответ 200 OK с переводом

ПОЧЕМУ ВАЖЕН:
  Граничные случаи важны для QA. Приложение должно уметь обрабатывать
  реалистичные сценарии: длинные документы, статьи и т.д.


2.3) test_boundary_case_unicode_text()
--------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Приложение корректно обрабатывает Unicode текст в форме
  • Русский текст (Привет мир 🌍), китайский (你好世界), арабский (مرحبا 👋)

КАК РАБОТАЕТ:
  • Цикл по трём Unicode текстам
  • Для каждого: отправляет POST запрос и проверяет 200 OK
  • Проверяет наличие мокированного ответа в результате

ПОЧЕМУ ВАЖЕН:
  Интеграционный тест Unicode. Гарантирует, что Flask форма и функция
  call_llm() правильно работают с многоязычным контентом.


2.4) test_different_http_error_codes()
--------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Приложение правильно обрабатывает HTTP ошибки от API (400, 401, 403, 404, 500)
  • Пользователь видит сообщение об ошибке, а не краш

КАК РАБОТАЕТ:
  • Для каждого кода ошибки:
    - Мокирует API для возврата этого кода
    - Отправляет POST запрос в режиме 'auth'
    - Проверяет статус 200 (приложение не упало)
    - Проверяет текст ответа содержит слово "error"

ПОЧЕМУ ВАЖЕН:
  Интеграционный тест обработки ошибок. Приложение должно gracefully
  обрабатывать сбои внешнего API.


3. CYPRESS E2E TESTS (End-to-End тесты)
========================================

Файл: cypress/e2e/translator_critic.cy.js

Все e2e тесты используют Cypress для автоматизации браузера. Каждый тест
открывает приложение, взаимодействует с UI элементами и проверяет результаты.


3.1) Test all language options work correctly
---------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Все 4 языка в выпадающем списке выбираются и работают
  • UI корректно обновляется при выборе каждого языка

КАК РАБОТАЕТ:
  • Устанавливает mock mode (переключатель #mode_mock)
  • Цикл по 4 языкам
  • Для каждого языка:
    - Вводит текст в textarea (#source_text)
    - Выбирает язык из select (#target_lang)
    - Кликает кнопку "Перевести" (#btn_translate)
    - Проверяет, что на странице появилось "Mocked Translation"

ПОЧЕМУ ВАЖЕН:
  UI тест. Гарантирует, что пользователь может выбрать любой язык и
  получить результат.


3.2) Test mode switching between MOCK, no auth, and auth
-------------------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Приложение имеет 3 режима работы
  • Пользователь может переключаться между режимами
  • Каждый режим работает (mode_mock, mode_no_auth, mode_auth)

КАК РАБОТАЕТ:
  • Переключает радиокнопку mode_mock, вводит текст, кликает "Перевести"
  • Проверяет результат (Mocked Translation)
  • Переключает на mode_no_auth, повторяет
  • Переключает на mode_mock, повторяет
  • Использует force: true для переключения радиокнопок (Bootstrap radio buttons)

ПОЧЕМУ ВАЖЕН:
  Функциональный тест. Проверяет одну из ключевых фич приложения:
  три режима работы для разных сценариев (разработка, тестирование, production).


3.3) Test Unicode and special characters in translation
------------------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • UI может обработать Unicode символы в поле ввода
  • Русский текст (Привет мир 🌍) и эмодзи (👋 Hello 😀)

КАК РАБОТАЕТ:
  • Включает mock mode
  • Вводит русский текст с эмодзи: "Привет мир 🌍"
  • Кликает "Перевести"
  • Проверяет "Mocked Translation"
  • Повторяет с текстом "👋 Hello 😀"

ПОЧЕМУ ВАЖЕН:
  E2E тест многоязычности в UI. Браузер должен корректно отправлять
  Unicode символы на сервер.


3.4) Test form field clearing after submission
----------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Состояние формы после отправки
  • Поле textarea существует
  • Выбранный язык остаётся после отправки

КАК РАБОТАЕТ:
  • Вводит текст в #source_text
  • Выбирает язык "Английский"
  • Кликает "Перевести"
  • Проверяет, что #source_text существует (не удален)
  • Проверяет, что #target_lang всё ещё имеет значение "Английский"

ПОЧЕМУ ВАЖЕН:
  UX тест. Пользователь может быстро отправить несколько переводов
  (форма не очищается полностью).


3.5) Test button states and interaction
--------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Кнопки "Перевести" и "Судить" видимы и включены
  • Кнопки остаются в корректном состоянии после клика

КАК РАБОТАЕТ:
  • Проверяет, что #btn_translate и #btn_judge видимы и не disabled
  • Кликает кнопку "Перевести"
  • Проверяет, что обе кнопки остаются видимы и активны

ПОЧЕМУ ВАЖЕН:
  UI тест доступности. Пользователь должен видеть и иметь возможность
  кликнуть на кнопки.


3.6) Test sequential submissions (submit form multiple times)
-----------------------------------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Форму можно отправлять несколько раз
  • Каждая отправка работает корректно
  • Нет состояния, которое "портит" последующие отправки

КАК РАБОТАЕТ:
  • 1-я отправка: вводит "First text", проверяет результат
  • Очищает поле, вводит "Second text", проверяет результат
  • Очищает поле, вводит "Third text", проверяет результат
  • После каждой проверяет, что "Mocked Translation" видна

ПОЧЕМУ ВАЖЕН:
  Сценарий реального пользователя. Человек может перевести несколько
  разных текстов подряд. Тест проверяет, что нет утечек состояния.


3.7) Test very long text submission
----------------------------------
ЧТО ПРОВЕРЯЕТ:
  • Форма может обработать очень длинный текст (5000 символов)
  • UI не зависает и не падает

КАК РАБОТАЕТ:
  • Создаёт строку из 5000 символов 'A'
  • Вводит её в #source_text (с delay: 0 для скорости)
  • Кликает "Перевести"
  • Проверяет, что "Mocked Translation" видна

ПОЧЕМУ ВАЖЕН:
  Граничный case для UI. Пользователь может захотеть перевести
  длинный документ. Приложение должно это поддерживать.


================================================================================
КАК ЗАПУСТИТЬ ТЕСТЫ
====================

ЗАПУСК ВСЕХ PYTHON ТЕСТОВ:
  cd /workspaces/AI.J.3.4
  python3 -m pytest tests/ -v

ЗАПУСК ТОЛЬКО UNIT ТЕСТОВ:
  python3 -m pytest tests/unit/test_app.py -v

ЗАПУСК ТОЛЬКО ИНТЕГРАЦИОННЫХ ТЕСТОВ:
  python3 -m pytest tests/integration/test_routes.py -v

ЗАПУСК CYPRESS ТЕСТОВ (требуется запущенное приложение):
  # Сначала запустите Flask приложение в отдельном терминале:
  cd /workspaces/AI.J.3.4/src
  python3 app.py
  
  # В другом терминале запустите Cypress:
  cd /workspaces/AI.J.3.4
  npx cypress run --spec "cypress/e2e/translator_critic.cy.js" --browser electron

ЗАПУСК CYPRESS В ИНТЕРАКТИВНОМ РЕЖИМЕ:
  npx cypress open


================================================================================
ТЕХНИЧЕСКИЕ ДЕТАЛИ
===================

MOCKING СТРАТЕГИЯ:

1. UNIT TESTS (tests/unit/test_app.py):
   - Используют unittest.mock.patch() для замены requests.post()
   - Создают фальшивые ответы сервера без реальных HTTP запросов
   - Быстрые (0.27 секунды для 6 тестов)

2. INTEGRATION TESTS (tests/integration/test_routes.py):
   - Используют Flask test_client() для симуляции HTTP запросов
   - Также используют unittest.mock.patch() для замены requests.post()
   - Тестируют полный путь: форма -> маршрут -> call_llm()
   - Не требуют запущенного сервера

3. CYPRESS E2E TESTS (cypress/e2e/translator_critic.cy.js):
   - Требуют запущенного Flask сервера на http://localhost:5000
   - Используют Cypress mock mode (встроенное в приложение)
   - Используют cy.intercept() для перехвата и замены API запросов
   - Полная эмуляция пользовательского поведения в браузере

ЗАВИСИМОСТИ:

Python:
  - pytest 7.0+ (фреймворк для тестирования)
  - unittest.mock (встроенный модуль для mocking)
  - Flask (веб-фреймворк, используется test_client)

JavaScript/Node:
  - cypress 15.7.1 (e2e testing фреймворк)
  - npm (менеджер пакетов)

РЕЖИМЫ РАБОТЫ ПРИЛОЖЕНИЯ:

Приложение поддерживает 3 режима в функции call_llm():

1. mode='mock':
   Возвращает жёстко закодированные ответы:
   - Для модели "Qwen" → "Mocked Translation: The sun is shining."
   - Для модели "claude" → "Mocked Grade: 9/10. Fluent and accurate."
   Не требует API ключа, работает offline.

2. mode='no_auth':
   Вызывает реальное API без Authorization заголовка.
   Полезно для тестирования на staging серверах.

3. mode='auth':
   Вызывает реальное API с Authorization: Bearer <API_KEY> заголовком.
   Требует валидный MENTORPIECE_API_KEY в окружении.


================================================================================
ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
=====================

СЦЕНАРИЙ 1: Новый разработчик проверяет, что функция работает
  python3 -m pytest tests/unit/test_app.py::test_call_llm_unicode_and_special_characters -v

СЦЕНАРИЙ 2: QA проверяет интеграцию между компонентами
  python3 -m pytest tests/integration/test_routes.py -v

СЦЕНАРИЙ 3: CI/CD pipeline перед deployment
  python3 -m pytest tests/ -v && npx cypress run

СЦЕНАРИЙ 4: Разработчик отлаживает конкретный UI сценарий
  npx cypress open
  (открывается интерактивный интерфейс Cypress)


================================================================================
КАЧЕСТВО И НАДЁЖНОСТЬ
=====================

ВСЕ ТЕСТЫ ИСПОЛЬЗУЮТ:
  ✓ Явные селекторы (element IDs вместо текста)
  ✓ Mocks вместо реального API (гарантированные результаты)
  ✓ Правильные ассерты (проверка ровно того, что нужно)
  ✓ Независимость (каждый тест может запуститься отдельно)
  ✓ Быстроту (вся suite за ~35 секунд)

УСТОЙЧИВОСТЬ К ОШИБКАМ:
  ✓ Нет зависимостей от времени (нет sleep, ждём явно)
  ✓ Нет зависимостей от порядка выполнения
  ✓ Каждый тест подготавливает своё окружение

ПОКРЫТИЕ:
  ✓ Нормальные потоки работы (успешные переводы)
  ✓ Граничные случаи (длинные тексты, Unicode)
  ✓ Обработка ошибок (HTTP 400-503)
  ✓ Состояние приложения (переключение режимов)
  ✓ Многоязычность (4 языка)


================================================================================
КЛЮЧЕВЫЕ ВЫВОДЫ
================

Эта тестовая система обеспечивает:

1. КАЧЕСТВО КОДА
   Тесты ловят ошибки до того, как они попадут в production

2. УВЕРЕННОСТЬ В ФУНКЦИОНАЛЬНОСТИ
   Все ключевые сценарии покрыты тестами

3. БЫСТРУЮ ОБРАТНУЮ СВЯЗЬ
   За 35 секунд видно, всё ли работает

4. ДОКУМЕНТИРОВАНИЕ
   Тесты показывают, как использовать API и UI

5. ПРЕДОТВРАЩЕНИЕ РЕГРЕССИЙ
   Если вы сломаете что-то старое, тесты это покажут

Тесты написаны с расчётом на обучение QA инженеров, поэтому:
  • Хорошо организованы по смыслу
  • Имеют подробные комментарии
  • Следуют лучшим практикам pytest и Cypress
  • Используют разнообразные техники (parametrize, fixtures, mocks)

================================================================================
QAsaurus
================================================================================

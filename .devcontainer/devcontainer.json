{
  "name": "AI.J.3.4 Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "forwardPorts": [5000],
  "postCreateCommand": "python3 -m pip install -r requirements.txt && npm install",
  "remoteUser": "root"
}

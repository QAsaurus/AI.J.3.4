# AI Translator & Critic

Flask веб-приложение для перевода текста и оценки качества перевода с использованием LLM (Language Learning Models) через MentorPiece API.

## 🚀 Запуск приложения

### Установка зависимостей
```bash
cd /workspaces/AI.J.3.4
python3 -m pip install -r requirements.txt
```

### Запуск сервера
```bash
cd src
python3 app.py
```

Приложение запустится на `http://localhost:5000`

**Вывод в консоль:**
```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

### Остановка сервера
Нажмите `Ctrl+C` в терминале где запущен сервер.

---

## 🧪 Автотесты (Python + pytest)

### Что проверяют?

**Unit-тесты (`tests/unit/test_app.py`)** — тестируют функцию `call_llm()`:
- ✅ Mock режим возвращает детерминированные ответы без сетевых вызовов
- ✅ Auth режим отправляет запросы с Authorization заголовком
- ✅ Загрузка API ключа из переменных окружения
- ✅ Обработка ошибок сети (RequestException)
- ✅ Все сетевые вызовы мокируются (`unittest.mock.patch`)

**Integration-тесты (`tests/integration/test_routes.py`)** — тестируют Flask маршруты:
- ✅ GET / возвращает форму с полями
- ✅ POST с текстом и языком возвращает перевод и оценку (в mock режиме)
- ✅ Пустой текст показывает ошибку "Please provide text to translate"
- ✅ Сетевые ошибки обрабатываются корректно
- ✅ Отсутствие API ключа показывает ошибку (в auth режиме)
- ✅ Используется Flask `test_client()` — не требует running сервера

### Запуск тестов
```bash
cd /workspaces/AI.J.3.4

# Все тесты (unit + integration)
python3 -m pytest -q

# Только unit-тесты
python3 -m pytest tests/unit/ -v

# Только integration-тесты
python3 -m pytest tests/integration/ -v

# Конкретный тест
python3 -m pytest tests/integration/test_routes.py::test_translation_and_judgment_flow -v
```

**Результат:** 8 passed in 0.23s ✅

---

## 🎯 Cypress E2E тесты

### ❌ Проблема в Codespaces: интерактивный режим требует Xvfb

**Ошибка при `npx cypress open`:**
```
✖ Your system is missing the dependency: Xvfb
Error: spawn Xvfb ENOENT
```

**Решение:** Контейнер Codespaces уже имеет Dockerfile с установленным Xvfb! Вам просто нужно **пересобрать контейнер** в VS Code.

### ✅ Как запустить интерактивный режим Cypress (пошагово для новичков)

#### Шаг 1️⃣: Пересобрать контейнер (один раз)

**На локальной машине с VS Code:**
1. Откройте Codespace или локальный проект в VS Code (где лежат файлы проекта)
2. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
3. Напишите: `Rebuild Container`
4. Нажмите Enter
5. **Ждите** — процесс пересборки займёт 3-5 минут (идёт загрузка Python 3.12, Node.js 20, Xvfb и Cypress)
6. Когда закончится, контейнер перезагрузится автоматически

**Что происходит:**
- VS Code скачивает новый образ Ubuntu с Python и Node.js
- Устанавливает Xvfb (виртуальный экран, нужен для Cypress GUI)
- Устанавливает все GUI зависимости для браузеров
- Устанавливает Python и npm пакеты

#### Шаг 2️⃣: Запустить приложение

**Откройте новый терминал в VS Code:**
```bash
cd src
python3 app.py
```

Вы должны увидеть:
```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

**Не закрывайте этот терминал!** Оставьте Flask работать.

#### Шаг 3️⃣: Запустить Cypress интерактивно

**Откройте второй терминал (+ кнопка в VS Code терминалах):**
```bash
cd /workspaces/AI.J.3.4
npx cypress open
```

**Должно откроться окно Cypress Test Runner:**
- Слева видите приложение в браузере
- Справа видите список тестов
- Можете кликнуть на тест и смотреть как он выполняется step-by-step

#### Шаг 4️⃣: Выполнить тесты

В Cypress окне:
1. Нажмите на тест `Successful translation and evaluation`
2. Смотрите как он вводит текст, нажимает кнопки, проверяет результаты
3. Нажмите на второй тест `API failure handling`
4. Смотрите как он тестирует обработку ошибок

**Супер удобно!** Видите в реальном времени каждый шаг теста.

---

### 📋 Альтернативные варианты (если пересборка не сработала)

#### Вариант 1: Запуск тестов headless (без UI, быстро)
```bash
cd /workspaces/AI.J.3.4

# В одном терминале запустите Flask:
cd src && python3 app.py

# В другом терминале запустите Cypress headless:
npx cypress run --spec "cypress/e2e/translator_critic.cy.js"
```

**Результат:** Тесты выполнятся автоматически и покажут результаты. Никакого UI, но работает везде.

#### Вариант 2: GitHub Actions (тесты запускаются автоматически при push)
- Workflow добавлен в `.github/workflows/cypress-e2e.yml`
- Каждый раз когда вы делаете `git push`, GitHub автоматически запускает тесты
- Результаты видны в GitHub → Actions tab
- Если тест упадёт, получите уведомление

Как это посмотреть:
1. Откройте репозиторий на GitHub
2. Перейдите на вкладку **Actions**
3. Выберите последний workflow run
4. Смотрите логи и результаты тестов

### Ручной запуск workflow (без push)
Теперь workflow поддерживает ручной запуск из веб-интерфейса GitHub — это удобно, если вы хотите проверить тесты без пуша.

Пошагово:
1. Откройте репозиторий на GitHub
2. Нажмите вкладку **Actions**
3. В списке workflow найдите **Cypress E2E Tests** (файл: `.github/workflows/cypress-e2e.yml`)
4. Справа нажмите кнопку **Run workflow**
5. В выпадающем окне выберите ветку (`main`) и нажмите **Run workflow**
6. Дождитесь выполнения — результаты появятся в том же интерфейсе (логи, артефакты)

Примечание: для ручного запуска у вас должны быть права на запуск Actions в репозитории (обычно владелец или коллаборатор).

---

### 🔧 Что это за файлы?

| Файл | Для чего |
|------|----------|
| `.devcontainer/Dockerfile` | Описание образа контейнера (Python, Node, Xvfb, GUI зависимости) |
| `.devcontainer/devcontainer.json` | Настройки контейнера (какой Dockerfile использовать, какие расширения VS Code устанавливать) |
| `.github/workflows/cypress-e2e.yml` | Инструкция для GitHub запускать тесты автоматически при push |

---

## 📋 Структура проекта

```
/workspaces/AI.J.3.4/
├── .env                           # Конфигурация (в .gitignore)
├── .env.example                   # Шаблон для копирования
├── requirements.txt               # Python зависимости
├── package.json                   # NPM зависимости (для Cypress)
├── cypress.config.js              # Конфигурация Cypress
│
├── src/
│   ├── app.py                     # Flask приложение с call_llm()
│   └── templates/
│       └── index.html             # UI форма с поддержкой 3 режимов
│
├── tests/
│   ├── unit/
│   │   └── test_app.py            # Тесты call_llm() с моками
│   ├── integration/
│   │   └── test_routes.py         # Тесты Flask маршрутов
│   └── UNIT_TESTS.md              # Документация тестов
│
└── cypress/
    └── e2e/
        └── translator_critic.cy.js # E2E тесты интерфейса
```

---

## 🔧 Режимы работы приложения

### 1. MOCK режим (по умолчанию)
- **Использование:** Локальное тестирование без API ключа
- **Ответы:** Жёсткие mock'и
  - Перевод: `"Mocked Translation: The sun is shining."`
  - Оценка: `"Mocked Grade: 9/10. Fluent and accurate."`
- **API ключ:** Не требуется
- **Сетевые вызовы:** Нет

### 2. API без Auth
- **Использование:** Тестирование endpoint'а без авторизации
- **Заголовок:** Content-Type только
- **API ключ:** Не требуется
- **Сетевые вызовы:** Да, на `https://api.mentorpiece.org/v1/process-ai-request`

### 3. API с Auth
- **Использование:** Полный режим с авторизацией
- **Заголовок:** `Authorization: Bearer {MENTORPIECE_API_KEY}`
- **API ключ:** **Требуется!** Установите в `.env`
- **Сетевые вызовы:** Да, с Authorization

---

## 📝 Краткая справка команд

```bash
# === ЗАПУСК ПРИЛОЖЕНИЯ ===
cd src && python3 app.py                    # Запуск Flask сервера

# === PYTHON ТЕСТЫ ===
python3 -m pytest -q                        # Все тесты
python3 -m pytest tests/unit/ -v            # Только unit
python3 -m pytest tests/integration/ -v     # Только integration

# === CYPRESS ТЕСТЫ ===
npm install                                 # Установка зависимостей
npx cypress open                            # Интерактивный режим
npx cypress run --spec "cypress/e2e/translator_critic.cy.js"  # Headless
npm run cypress:open                        # Через npm скрипт
npm run cypress:headless                    # Через npm скрипт
```

---

## 🎓 Для начинающих QA инженеров

- **Unit-тесты** показывают как мокировать внешние API с `unittest.mock`
- **Integration-тесты** показывают как тестировать Flask маршруты с `test_client()`
- **Cypress тесты** показывают как писать e2e тесты с `cy.intercept()` для перехвата сетевых запросов
- Все файлы содержат подробные комментарии для обучения
